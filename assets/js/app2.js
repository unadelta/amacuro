function redirigir4() {
    var select = document.getElementById("miSelect4");
    var url = select.value;
    if (url) {
        window.location.href = url;
    }
}

function redirigir3() {
    var select = document.getElementById("miSelect3");
    var url = select.value;
    if (url) {
        window.location.href = url;
    }
}


function redirigir2() {
    var select = document.getElementById("miSelect2");
    var url = select.value;
    if (url) {
        window.location.href = url;
    }
}

function redirigir0() {
    var select = document.getElementById("miSelect0");
    var url = select.value;
    if (url) {
        window.location.href = url;
    }
}


function redirigir() {
    var select = document.getElementById("miSelect");
    var url = select.value;
    if (url) {
        window.location.href = url;
    }
}

function redirigir1() {
    var select = document.getElementById("miSelect1");
    var url = select.value;
    if (url) {
        window.location.href = url;
    }
}

